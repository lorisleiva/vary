<?php


it('passes dummy tests', function () {
    expect(true)->toBeTrue();

    // Vary::file('path')->replace('foo', 'bar')->save();
    // Vary::file('path')->mustache('foo', 'bar')->save();
    // Vary::file('path')->mustacheAll(['foo' => 'bar'])->save();
    // Vary::string('some_string')->mustacheAll(['foo' => 'bar'])->save('some_path');
});
